# EVA-Finance

Local-first financial signal analysis system.

## Quick start
1. Copy `.env.example` → `.env`
2. Adjust secrets as needed
3. `docker compose up -d`
4. Open Metabase at http://localhost:3000
